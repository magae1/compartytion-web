import HeaderTailWrapper from "@/components/HeaderTailWrapper";
import HeaderButtonGroup from "@/components/HeaderButtonGroup";

export default function HeaderTail() {
  return (
    <HeaderTailWrapper>
      <HeaderButtonGroup />
    </HeaderTailWrapper>
  );
}
